# order-system
system for placting orders






# applyging migratins
while being in service folder
./mvnw clean package -DskipTests
docker compose build --no-cache <service-name>
docker compose up -d <service-name>
